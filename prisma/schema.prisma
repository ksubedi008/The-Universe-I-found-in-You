generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
}

model Message {
  id        String   @id @default(uuid())
  content   String
  createdAt DateTime @default(now())
}

model Memory {
  id          String   @id @default(uuid())
  title       String
  description String?
  imageUrl    String?
  createdAt   DateTime @default(now())
}

model Song {
  id        String   @id @default(uuid())
  title     String
  url       String   // publicly accessible URL (e.g. /music/filename.mp3 or a blob URL)
  createdAt DateTime @default(now())
}
